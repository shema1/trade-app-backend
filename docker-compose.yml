version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - MONGODB_URI=mongodb://mongodb:27017/trade-app
      - NODE_ENV=production
    depends_on:
      - mongodb
    networks:
      - trade-app-network

  mongodb:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    networks:
      - trade-app-network

networks:
  trade-app-network:
    driver: bridge

volumes:
  mongodb_data: 